# 📝 ToDo CLI Application

A simple command-line ToDo application written in Python.
Allows you to manage tasks directly from the terminal.
Tasks are stored persistently using a JSON file.

## 🚀 Features

- Add new tasks
- View task list
- Remove tasks
- Persistent storage (JSON)

## 🛠 Technologies

- Python 3.14
- Standard Library only
- CLI interface

## 📦 How to run

```bash
python main.py